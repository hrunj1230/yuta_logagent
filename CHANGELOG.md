# 변경 이력

이 프로젝트는 버전 태그 없이 개발됐다. 아래 `v0.1`~`v0.6`은 커밋 74개(2026-07-09 ~ 08-05)와
`docs/` 아래 작업 기록을 사후에 의미 단위로 묶어 붙인 이름이다.

각 항목은 **무엇이 조용히 실패했나 → 근본 원인 → 어떻게 고쳤나** 순으로 쓴다.
결과물은 코드에 있으므로, 여기 남기는 것은 코드를 봐서는 알 수 없는 것 — 어떤 결론이
뒤집혔고, 무엇을 보지 않고 판단했고, 왜 그 수정을 택했는지다.

수치는 전부 실측이다. 추정한 값은 추정이라고 적었다.

---

## v0.6 — 조용히 실패하던 수집 파이프라인 (현행, 2026-08-04 ~ 08-05)

이 구간의 공통 주제는 하나다. **실패가 성공처럼 보였다.**

| 무엇이 | 어떻게 보였나 |
|---|---|
| 0건 수집 | 성공 |
| 39/44건 잘림 | 정상 응답 |
| 커밋 유실 | 통계에 안 잡힘 |
| 테스트 깨짐 | 통과 |
| 저장소 갱신 안 됨 | 에러 없음 |

그래서 이 구간 수정의 공통점이 **"실패를 소리 나게 만들기"**다 — `skip_reasons` 집계,
`commit_shas`·`session_ids` 검산용 메타데이터, 경로가 없으면 예외, 제외 건수 명시, 계약 테스트.

```
"2026-07-28 일지 써줘"

before  44건이 저장돼 있는데 5건만 도달 — 39건(89%)이 에러도 경고도 없이 사라짐
        남은 5건은 "중요한 5건"이 아니라 "먼저 저장된 5건"
after   44건 전부 도달. 예산을 넘기면 제외 건수와 담긴 기간을 응답에 명시
```

- **`if not repo_dir.exists(): clone(...)` 에 `else`가 없었다** (C2, `b55968b`) — 이미 받아둔
  저장소는 **한 번도 갱신되지 않았다.** 에러도 경고도 없이, clone 시점 이후의 커밋이 영원히
  수집되지 않는다. `_sync_repo` 공통 함수로 통합(+70/−33). `tests/unit/test_sync_repo.py`에
  회귀 테스트를 붙인 뒤 **수정 전 코드로 되돌려 5건이 FAIL하는 것을 확인**해, 테스트가 실제로
  이 버그를 잡는지 검증했다.

- **검색이 `limit=5`로 조용히 잘렸다** (L3, `c32b7ff`) — 가장 실질적인 피해였다. 원인이 셋
  겹쳐 있었다: ① LLM이 `reference_len`을 임의로 5로 설정 ② 날짜 필터는 정확일치라 문서 사이에
  우열이 없어 순위가 무의미 ③ 그래서 남는 것이 "중요한 5건"이 아니라 **"먼저 저장된 5건"**.
  나중에 임베딩한 소스가 통째로 밀려난다. → `reference_len` 제거, `MAX_RESULT_CHARS` 글자
  예산 도입, 축약 단계, **제외 건수와 담긴 기간 명시.**

- **커밋 1건 = 문서 1개 구조가 부피의 82%를 메타데이터로 채우고 있었다** (`b55968b`) — 커밋
  67건 14,221자 중 **53%가 파일 변경 목록, 29%가 날짜+작성자 반복**이었다. 하루당 문서 1개로
  묶어 **67개 → 8개(69% 감소).**
  발견한 부작용: 하루 요약이 `CHUNK_SIZE=2000`에 걸려 4청크로 쪼개졌다. 제목 줄과 본문이
  갈라져 고아 청크가 된다 → `DAILY_SUMMARY_TYPES`로 분할에서 제외(날짜 필터로 조회하므로
  길어도 불리하지 않다).
  검증: 원본 67개 커밋 중 merge 2건 + 빈 커밋 1건을 뺀 **64개 sha가 저장분과 완전 일치.**

- **빈 문서를 저장하면서 유실은 세지 않았다** (C4·C5, `b55968b`) — merge 커밋은 `git log
  --numstat`이 변경 내역을 아무것도 내보내지 않는다(git 기본 동작). 그래서 텅 빈 커밋 문서가
  저장됐고, 동시에 제외 건수를 세는 코드가 없어 **유실이 통계에 잡히지 않았다.** 둘이 맞물려
  "빠진 게 없어 보이는" 상태를 만든다. → merge·빈 커밋을 제외하고 `skip_reasons`에 사유별로
  기록.

- **일지의 「막힌 것 / 예정」이 늘 비어 있었다** (C1, `aec9f4c`) — 커밋과 문서에는 그 정보가
  없기 때문이다. Claude Code 트랜스크립트를 조사하니 12세션 19MB 중 **사람이 실제로 친 말은
  179건 17KB(1%)**뿐이었다(나머지는 도구 호출·결과·thinking). 약 567:1 압축.
  → `src/tools/chatlog.py` 신규. `MIN_TURN_CHARS=12`(선택지 응답 제외),
  `MAX_TURN_CHARS=600`(붙여넣기 상한). 도구가 끼워 넣은 텍스트가 하루치의 67%를 차지해
  접두사로 걸러낸다.

- **진단을 스스로 뒤집었다 — 모델은 `user_id`를 한 번도 빠뜨리지 않았다** — 최초 진단은
  "모델이 `user_id`를 자꾸 빠뜨린다"였고, `Annotated[str, InjectedState("user_id")]`로
  스키마에서 숨기는 방안까지 설계했다. LangSmith로 실측하니(2026-07-22~08-04, 도구 호출 177건):

  | 경로 | 호출 | `user_id` 누락 |
  |---|---|---|
  | 스크립트 직접 호출 | — | **14건 (전부)** |
  | 에이전트 경유 | 23건 | **0건** |

  진짜 원인은 `test_log_tools.py`가 커밋 `9c3f4e1`의 시그니처 변경을 따라가지 못한 낡은
  호출이었다. → `tests/unit/test_log_tool_contract.py`(계약 테스트)로 시그니처를 스키마
  수준에서 못 박았다.
  **교훈: 화면에 보이는 실패 건수만으로 원인을 귀속하면 안 된다. 어느 경로에서 났는지를
  갈라야 한다.**

- **항상 통과하던 가짜 테스트** (`81f481c`) — `test_log_tools.py`는 **assert 0개, except
  8개**였다. 무엇이 깨져도 통과한다. 게다가 실제 LLM을 호출하고 사용자 데이터를 오염시킨다.
  위 `user_id` 사건이 오래 안 잡힌 이유가 이것이다. → `pyproject.toml`에
  `testpaths = ["tests"]`를 넣어 자동 수집에서 제외.

  | | 전 | 후 |
  |---|---|---|
  | 테스트 | 76건 | 70건 |
  | 실행 시간 | 49.7s | **19.1s** |
  | LLM 호출 | 5회 | **0회** |

- **git log 명령을 세 번에 걸쳐 줄였다** (`5e910f3`) — 1차: `--numstat` → `--name-only`
  (전체 4,393자 중 숫자가 10%를 차지했고, "+6368줄" 같은 큰 값이 실은 문서 파일 추가였다).
  2차: `--graph --all` 제안이 나왔으나 **실측으로 반박했다** — `--graph`는 그래프 문자
  (`*`, `|\`, `|/`)를 줄 앞에 붙여 파서의 마커 탐지가 실패한다(71줄 파싱 실패, 마커가 맨 앞인
  줄 0개). 3차(최종): `--mirror` → `--filter=blob:none --no-checkout`. 커밋 메타데이터만
  읽으므로 파일 본문(blob)을 아예 받지 않는다. **756K → 168K (78% 감소).**

- **조회 결과가 대화에 눌러앉아 매 호출마다 재전송됐다** (`a1d8fab`) — ChromaDB는 날짜로
  거르는데, **조회 결과 10,321토큰이 `InMemorySaver`에 남아 이후 모든 호출에 재전송**됐다.

  | 방식 | 토큰 | 비용 |
  |---|---|---|
  | 같은 thread 5회 | 275,265 | $0.83 |
  | 새 대화마다 | 63,285 | $0.19 |

  시나리오 실측에서 나온 비직관적 결론: **스레드 분리와 프롬프트 캐싱은 보완재가 아니라
  대체재다.** 분리하고 나면 매번 새 도구 결과라 캐시 효율이 12%로 떨어진다.
  → `prune_tool_traffic` 노드로 도구 호출·결과를 저장 직전에 제거 + 시스템 메시지에
  `cache_control`. 요청당 새로 과금되는 토큰이 **11,742 → 531.**

- **기간 일지를 하루씩 쪼개 생성** (`b2b7b2c`) — `generate_daily_journals`. 기간을 한 번에
  조회하면 아래 「알려진 한계」의 `SHRINK_STEPS` 문제에 걸린다. 날짜별로 순회해 우회한다.

---

## v0.5 — 일지 품질을 측정할 수 있게 만들다 (2026-08-03 오후 ~ 08-04)

일지 품질을 재려고 평가 시스템을 만든 구간이다. **문제는 재는 대상이 아니라 자[尺]에서
나왔다.** 상세는 [`docs/일지평가-함정기록.md`](docs/일지평가-함정기록.md)(함정 9개)와
[`docs/일지-모델비교-기록.md`](docs/일지-모델비교-기록.md)에 있다.

```
"세 모델 중 뭐가 제일 낫나"

before  Haiku·Sonnet·Opus 차이 최대 3.7%p — "모델 차이는 무의미하다"
after   Haiku 60.3% / Sonnet 68.0% / Opus 75.7% — 15.4%p
        결론을 바꾼 건 모델이 아니라 분모였다
```

- **기준을 정하지 않고 지표부터 만들었다** — "좋은 일지"의 정의 없이 커버리지·근거성부터
  계산했다. 나중에 기준을 정해 프롬프트에 박았다 — **담을 것**(한 일 / 막힌 것 / 정한 것 /
  예정 / 다룬 주제), **버릴 것**(자료 밖 설명, 개념 상세 설명, 맥락 없는 숫자, 참조 링크,
  복원 불가 조각).

  | 지표 | v1 (옛 기준) | v2 (새 기준) |
  |---|---|---|
  | 커밋 반영 | 98% | **100%** |
  | 막힌 것 포착 | 90% | **96%** |
  | 환각 없음 | 93% | **96%** |
  | 평균 길이 | 803자 | 574자 (29%↓) |

  **모델을 바꾸지 않고 프롬프트만 고쳐 얻은 결과다.** 나중에 모델을 올려서 얻은 것은 비용
  5배에 1~4%p였다.

- **지표의 분모에 내 문체가 섞여 있었다** — 커버리지 22.3%가 나와 "많이 놓쳤다"고 판단했다.
  누락 146개를 뜯어보니 **98개는 진짜 누락, 48개는 원자료에 없고 내 일지에만 있는 표현
  (내 문체)**이었다. 근거성 76.8%도 일반명사가 섞여 실제로는 ~97%였다. **두 지표가 반대
  방향으로 왜곡돼 있었다** — 커버리지는 낮게, 근거성은 높게.
  부수 원인: `_hit()`이 단순 부분문자열 매칭이라 원자료의 `overfitting`이 일지에서
  "오버피팅"으로 쓰이면 0점이었고, STOPWORDS가 부족해 `파일`·`추가`·`이랑` 같은 말이 분모를
  부풀렸다.

- **원자료를 잘라 읽고 "환각"이라 판정했다 — 두 번** — 4,000자 중 600자만 읽고 neo4j·chroma를
  환각으로 몰았다. **전부 원자료에 있었다.** 실제 환각은 `300차원`, `tensor` 정도뿐이었다.
  사람의 눈으로 환각을 판정하는 것이 안 된다는 근거가 여기서 나왔다.

- **분모가 0이라 자동 만점이 나오고 있었다**

  ```python
  score = hits / len(blockers) if blockers else 1.0   # ← 분모 0이면 100점
  ```

  정규식으로 뽑은 분모가 **55일 중 43일(78%)에서 0개**였다. 94~98%라는 높은 점수의 대부분이
  채점 결과가 아니라 자동 만점이었다.

- **LLM에게 매번 물으면 재현되지 않는다** — 분모를 LLM으로 뽑게 했더니 같은 자료인데 3번 돌려
  **6개, 10개, 8개**가 나왔다. 항목 자체도 실행마다 달라진다. 일지를 고치지 않아도 점수가
  30%p씩 움직인다. Sonnet 5·Opus 5는 `temperature`가 제거돼 결정론을 요청할 방법도 없다.

  | 방식 | 재현성 | 정확도 |
  |---|---|---|
  | 정규식 | 완벽 | 낮음 (분모 0으로 고정) |
  | LLM 매회 호출 | **없음** (6~10개) | 높음 |
  | **TREC 풀링** | 완벽 | 높음 |

  → `scripts/build_blocker_pool.py`가 Haiku·Sonnet·Opus 3개 모델로 각각 추출하고 로컬 임베딩
  코사인 유사도로 병합해 `<date>.blockers.json`에 **한 번만 만들어 파일로 고정한다.** 이후
  채점은 대조만 한다. LLM 호출 **165회(55일×3모델) → 0회**, 점수 변동 제거, **사람이 파일을
  열어 오탐을 지울 수 있다.** 병합 임계값 0.60은 실측으로 정했다 — 합쳐야 할 쌍은 0.64~0.80,
  아닌 쌍은 0.12~0.28에 분포해 그 사이가 넓게 비어 있다.
  여러 모델의 합집합을 쓰는 이유는 정보검색 평가(TREC)와 같다. **한 모델이 만든 목록으로
  채점하면 그 모델의 관점이 곧 정답이 되어 자기 자신에게 유리해진다.**

- **그래서 "모델 차이 없음"이라는 결론이 뒤집혔다** — 이 구간에서 가장 중요한 사건이다.
  합의 수준별로 나눠 보니 판정 근거가 분명해졌다:

  | support | 항목수 | Haiku | Sonnet | Opus |
  |---|---|---|---|---|
  | **3/3** (확실) | 57 | 70.2% | **80.7%** | 82.5% |
  | 2/3 | 68 | 66.2% | 72.1% | 80.9% |
  | **1/3** (오탐 의심) | 175 | 50.9% | **54.3%** | 70.3% |

  **Opus의 우위가 오탐 의심 구간에서 가장 크다(+19.4%p).** → **Sonnet 5 채택.** 확실한
  항목에서 Opus의 98% 수준(80.7 vs 82.5)을 내면서, 의심스러운 항목은 16%p 덜 집는다.
  `src/llm_router.py`의 `ANTHROPIC_MODEL`이 이것이다.

- **길이를 점수화해 비대칭이 생겼다** — `concise = min(1.0, 500 / length)`. **지표 자체가
  짧은 답을 선호하도록 설계돼 있었다.** Opus가 500자 지시에 890자를 쓴 것은 게을러서가 아니라
  **더 많이 찾아냈기 때문**이었다(같은 자료에서 「한 일」 4개, 「막힌 것」 6개 vs Haiku 각
  2개). 그런데 추가로 잡은 항목은 분모에 없어 점수에 반영되지 않고, 길이만 정확히 측정돼
  감점됐다. **측정 가능한 것만 벌하는 구조였다.** 웹 검색으로 확인하니 알려진 문제였다 —
  길이 제약 미준수(LIFEBench), 불완전한 정답셋(TREC pooling bias), verbosity
  bias(LLM-as-Judge). → `concise` 삭제, `length`는 참고값으로만, 프롬프트의 500자 제한도 폐지.

- **개념 노트 층을 성급히 설계했다** — "학습한 개념을 키워드로 저장하고 그 키워드로 원문을
  조회하자"는 구조를 설계했다. 실제로 세어 보니 55일에서 고유 용어가 **6,479개**였고, 빈도
  상위는 `피드백`(52일)·`https`(51일)처럼 개념이 아니라 노이즈였다. **빈도나 용어 추출로는
  개념과 노이즈를 구분할 수 없다.** 개념 자동 추출은 불가능하다고 결론짓고 일지가 쌓인
  다음으로 미뤘다.
  부수적으로 얻은 사실: 이름만 나열하는 것보다 **이름+한 줄 설명**이 검색 신호가 34~36%
  높다(0.3888→0.5231). 다만 항목이 많아지면 신호가 희석된다.

> **주의**: 이 구간의 산출물인 `scripts/build_blocker_pool.py`,
> `scripts/generate_journals.py`, `scripts/langsmith_eval.py`는 2026-08-05 기준 아직
> 커밋되지 않았다. 위 수치는 `docs/` 아래 작업 기록에 근거한다.

---

## v0.4 — 날짜 검색이 한 번도 걸리지 않고 있었다 (2026-07-31 ~ 08-03)

프로젝트의 핵심 컨셉은 "소스를 날짜 메타데이터와 함께 임베딩하고, 날짜로 검색해 일지를
생성한다"이다. **그런데 그 컨셉이 파이프라인에 실제로 구현되어 있지 않았다.**
상세는 [`docs/임베딩-파이프라인-문제분석.md`](docs/임베딩-파이프라인-문제분석.md).

```
"2026-07-22 일지 써줘"

before  청크 572개 중 510개(89%)가 Obsidian 플러그인 자바스크립트
        date 메타데이터를 가진 청크: 0개
        → 필터가 항상 빈 결과 → "작성 날짜: 2026-07-22" 라는 문자열을
          임베딩해 유사도 검색으로 조용히 폴백 → 사실상 무작위 결과
after   청크 572 → 124, 플러그인 청크 0, date 보유 100%
        chroma.sqlite3 12MB → 1.5MB
```

- **P1·P2·P3는 함께 고쳐야 했다** — 셋이 하나의 증상("날짜 검색 실패")을 만든다.

  ```
  P1 (필터 없음)  ─┐
                    ├─→ 날짜 검색이 무관한 코드를 반환
  P2 (날짜 없음) ─→ P3 (필터 실패 → 조용한 폴백) ─┘
  ```

  P2만 고치고 P1을 두면 정확도는 오르지만 여전히 플러그인 코드가 섞이고, P1만 고치고 P2를
  두면 코퍼스는 깨끗해지지만 날짜 조회는 여전히 불가능하다.

- **P1 — 수집에 필터가 없었다** — 확장자 화이트리스트에 `.js` 등 코드 확장자가 들어 있었고,
  `_collect_git_files()`와 `_collect_local_files()`가 모두 `rglob("*")`로 전체 트리를
  무조건 순회했다. 제외 디렉토리 규칙(`.obsidian`, `.git`, `node_modules`, `.venv`)도, 파일
  크기 상한도 없었다. 수집 대상 트리에 `theme.css` 1.6MB, `main.js` 316KB가 실재했다.
  → 확장자를 `.md`/`.txt`로 축소, 제외 디렉토리 도입, **날짜를 끝내 추출할 수 없는 파일은
  수집에서 제외**(이 규칙 자체가 보조 필터로도 기능한다).

- **P2 — 날짜 메타데이터가 아예 생성되지 않았다** — 메타데이터 키에 `date`가 없었다. 대신
  있던 `embedded_at`은 전체가 같은 값 — **임베딩을 실행한 시각**이지 문서가 작성된 날짜가
  아니다. `Today I Learn/2026_05_14.md`처럼 **파일명에 날짜가 명시돼 있는데도** 파싱하지
  않았다. → 파일명 → frontmatter → git 커밋 날짜 → mtime 순으로 시도하고 `YYYY-MM-DD`로
  통일. 순수 함수로 분리해(`src/tools/ingest_rules.py`) 단위 테스트를 먼저 썼다.

- **P3 — 실패가 `print("[DEBUG] ...")`로만 기록됐다** — 1차 필터가 존재하지 않는 키를
  조회하므로 결과가 항상 비어 있고, 2차로 `"작성 날짜: 2026-08-01"`이라는 **문자열 자체를
  임베딩해 의미 유사도 검색**을 수행했다. 날짜 문자열의 임베딩 벡터는 문서의 작성 날짜와
  아무 상관관계가 없다. 게다가 실패가 호출자에게 전달되지 않아 **검색이 실패했다는 사실
  자체가 드러나지 않았다.** → 필터 키 정정 + 무의미한 폴백 제거 + 실패를 명시적으로 반환.

- **P8 — 검증 중에야 드러난 것: git이 한글 경로를 8진수로 이스케이프한다** — `git log
  --name-only`는 기본 설정에서 비ASCII 경로를 `"\355\232\214\352\263\240..."` 형태로
  출력한다. 날짜 매핑이 실제 파일 경로와 매칭되지 않아 **한글 파일명 24개가 날짜를 얻지 못하고
  수집에서 탈락**했다. → `git -c core.quotepath=false`로 두 곳 수정. 이걸 고친 뒤 수집 대상이
  **35개 → 59개**로 늘고, 날짜를 못 붙인 파일은 0개가 됐다.

- **웹 UI에서 422가 성공으로 표시됐다** — `log_maker.html`이 `{req, thread_id}`를 보내는데
  백엔드 `QueryReq`는 `{message, user_id}`를 기대해 HTTP 422가 났다. 그런데
  `log_maker.html:375-380`이 **`response.ok`를 확인하지 않아** 422가 성공으로 보였다.
  파이썬에서 직접 부르면 임베딩·날짜 검색·일지 저장이 전부 정상이었고 브라우저 경로만 막혀
  있어 오래 발견되지 않았다.
  → **필드만 맞추지 않고 `log_maker` 자체를 폐기했다.** `/user/{user_id}` 채팅이 이미 정상
  동작하고 있어 화면을 두 벌 유지할 이유가 없었다.

- **같은 일을 하는 코드가 두 벌 있으면 한쪽만 고쳐진다** — 소스를 지워도 검색에 계속 나왔다.
  Agent 도구 쪽은 `_delete_source_embeddings`를 부르는데 **HTTP 삭제 경로는 SQLite 레코드만
  지우고 있었다.** 설정 화면 삭제와 Agent 도구의 동작이 다르다는 것이 단서였다.
  함께 드러난 것: `/settings` 500(절대 임포트), `/debugger` 500(템플릿 파일이 아예 없었음).

- **임베딩이 요청을 붙잡고 있어 채팅이 멈췄다** (`f42f00f`, `b19b2d6`) — 백그라운드 스레드로
  옮기고(0.06초 즉시 반환), 진행률을 DB 컬럼에 적고, 화면이 폴링하도록 했다(임베딩 중 2초 /
  유휴 15초). 여기서 **stale 판정**이 함께 필요했다 — 서버가 임베딩 도중 죽으면 상태가 영원히
  `IN_PROGRESS`로 남아 재시도가 막히기 때문이다. 30분을 넘기면 중단된 작업으로 본다.

- **P5·P6** — 파일 수정 시 이전 해시 청크가 남는 문제(delete-then-insert 도입), 생성된 일지가
  다시 임베딩되지 않아 요약 계층이 끊기던 문제(`maker_logfile`이 저장 후 재임베딩).

- **Router→Single 마이그레이션 잔재 제거** (`f8bee23`) — 아래 v0.2 참조. 실제로 지우기까지
  5일이 걸렸다.

---

<details>
<summary><b>v0.3 이전 — 배포, 초기 설계, 컨트롤러 두 갈래</b></summary>

## v0.3 — 배포 파이프라인, 그리고 4번 연속 다른 이유로 죽은 CI (2026-07-30)

`main`에 push하면 Docker 이미지를 빌드해 ECR에 올리고 EC2에 배포한다.
상세는 [`docs/cicd-setup.md`](docs/cicd-setup.md).

```
GitHub (main push)
  → GitHub Actions: docker build → ECR push (커밋 SHA 태그)
  → AWS SSM Send-Command로 EC2에 배포 스크립트 실행
  → EC2: docker pull → SSM Parameter Store에서 시크릿 주입 → 컨테이너 재기동
  → 헬스체크(HTTP 200) 통과 확인 후 종료
```

인증은 OIDC라 **장기 자격 증명이 어디에도 저장되지 않는다.** IAM Role의 trust policy를 이
저장소의 `main` 브랜치로만 제한했고, 권한은 ECR push + 해당 인스턴스 SSM 두 개뿐이다. 배포도
SSH가 아니라 SSM Run Command라 SSH 개인키를 GitHub에 넣을 필요가 없다.

1차 실행 시 **배포 자체는 성공했는데 파이프라인은 "실패"로 표시됐다.** 원인을 순서대로 넷
찾았다.

- **헬스체크가 너무 일찍 실행됐다** (`9a733c7`) — `docker run` 5초 뒤 바로 curl로 확인했는데,
  컨테이너가 HuggingFace 임베딩 모델을 로딩하는 데 그보다 오래 걸려 연결이 끊겼다. 스크립트
  상단의 `set -e` 때문에 그 실패로 **전체 배포 스크립트가 죽은 것으로 처리**됐다. → 최대 2분간
  5초 간격 재시도.

- **ECR 리포지토리가 태그 불변(IMMUTABLE)인데 `:latest`를 같이 push했다** (`f7efb55`) — 두
  번째 빌드부터 "이미 존재하는 태그는 덮어쓸 수 없다"로 실패. 배포 스크립트는 커밋 SHA 태그만
  쓰므로 `:latest` 자체가 애초에 불필요했다.

- **디스크 공간 고갈** (`ed9390d`) — 매 배포마다 커밋 SHA로 새 이미지(약 5.6GB, torch·
  sentence-transformers 포함)를 pull하는데, `docker image prune -f`는 dangling(태그 없는)
  이미지만 지워서 이전 커밋 태그 이미지들이 계속 쌓였다. **5번째 배포 만에 30GB 디스크가 가득
  차 `no space left on device`로 실패.** → `prune -af`로 바꾸고, 헬스체크 통과 **직후**로
  위치를 옮겨 배포 실패 시엔 롤백용 이전 이미지가 남게 했다.
  **교훈: 매번 새 태그로 이미지를 pull하는 파이프라인은 이미지 정리 로직이 필수다.** 로컬
  테스트 몇 번으로는 안 드러나고, 배포 횟수가 쌓여야 터지는 종류의 문제라 더 놓치기 쉽다.

- **데이터 영속성이 없었다** — 기존 수동 배포는 볼륨 마운트가 전혀 없어 컨테이너를 내릴 때마다
  `users.db`/`chroma_db`/`data`가 초기화되는 상태였다. → 영구 마운트 추가.

- **죽은 시크릿을 방치했다** — 초기에 "EC2에 SSH로 접속해 `.env`를 직접 심는 방식"을 먼저
  시도하면서 로컬 `.env`의 6개 키를 전부 GitHub Secrets에 등록했다. 이후 이미 구성돼 있던 SSM
  Parameter Store 기반 설계를 발견하고 그쪽으로 바꿨는데, **GitHub Secrets에 넣어둔 6개를
  정리하지 않고 그대로 뒀다.** 같은 API 키가 두 곳에 중복 저장된 채, 워크플로우는 그중 아무것도
  실제로 쓰지 않는 상태였다. 로테이션 시 한쪽만 갱신하기 쉽고, 어디가 진짜 소스인지 헷갈리고,
  노출 지점만 하나 더 생긴다.
  **교훈: 시크릿의 진실 공급원은 하나로 유지하고, 설계를 바꿀 때는 이전 설계의 흔적(사용하지
  않는 시크릿·변수·하드코딩된 값)을 반드시 같이 정리한다.** `EC2_HOST`, `EC2_USER`,
  워크플로우에 하드코딩돼 있던 인스턴스 ID(`3c9d8e1`)도 같은 이유로 함께 정리했다.

- **하드코딩된 `"user_hrun"` 컬렉션** (`0cd141a`) — 일지 검색 도구가 사용자와 무관하게 고정
  컬렉션을 보고 있었다. 다중 사용자를 표방하면서 실제로는 한 사람 것만 읽고 있었던 셈.
  `user_id`별로 스코프하도록 고쳤다. 같은 커밋에서 레거시 `src/controller.py`(465줄)를
  드롭했다.

---

## v0.2 — 도구 레이어, 그리고 컨트롤러를 두 갈래로 만들어 본 것 (2026-07-28)

하루에 커밋 43개가 들어간 구간이다. `src/tools/`를 세 층(`source` / `embedding` / `log`)으로
세우고, **그 위에 컨트롤러를 두 벌 만들었다.**

### 두 벌을 만든 이유

당시 구현 가이드가 Phase 3을 **"3.1 단일 Agent 방식 / 3.2 Router Agent 방식"**으로 나누고
Phase 4.3에서 비교하도록 되어 있었다. 설계 문서의 핵심 결정 사항 첫 줄이 "두 가지 방식 모두
구현"이다.

**A. `unified_controller_single.py`** (`3c79a59`~`83c978d`)

```
START → agent (도구 6개 전부 bind) ⇄ tools → END
```

LLM이 필요한 도구를 스스로 연쇄 호출한다.

**B. `unified_controller_router.py`** (`e579995`~`c262494`, 382줄)

```
START → router (요청 분류) ─┬→ source_agent    ⇄ source_tools    → END
                             └→ embedding_agent ⇄ embedding_tools → END
```

`router_llm.with_structured_output(RouteDecision)`으로 목적지를 정하고, sub-agent마다 다른
LLM을 붙였다(router·source는 `google_llm`, embedding은 `anthropic_llm`).

### 왜 Router를 버렸나

가이드에 적혀 있던 트레이드오프는 이랬다.

| | 장점 | 단점 |
|---|---|---|
| Router | 책임 분리, Agent 추가 쉬움, **Agent별 다른 LLM** | **코드량 2배**, 라우팅 오류 가능성 |

실제로 쓰면서 장점 셋 중 가장 큰 것이 먼저 사라졌다. `ab18a51`에서 **Gemini 쿼터 문제로
전부 Anthropic Claude로 전환**하면서 "Agent별 다른 LLM"이 무의미해졌다. 남은 것은 코드량
2배와 라우팅 오류 위험이었다.

경계 문제도 있었다. 실제 사용 흐름은 "이 저장소 추가해줘" → 등록 → **곧바로 임베딩**처럼 두
도메인을 넘나드는데, 라우팅은 요청을 한 도메인에 배정한다. 그래서 경계를 넘기 위한
`add_source_and_embed` 통합 도구를 따로 만들어야 했다(`20776b8`) — **라우팅으로 나눈 것을
도구로 다시 붙이는 셈**이었다. 단일 Agent 쪽은 `add_source_to_db` → `embed_source`를 그냥
연달아 부르면 되므로 이 도구가 필요 없다.

### 지우는 데 5일이 걸렸다

`0cd141a`(07-30)에서 `router.py`가 single만 import하도록 바꿔 **Router를 실질적으로 끊었지만,
파일은 남아 있었다.** 실제 삭제는 `f8bee23`(08-03)이다. 그 사이에 드러난 것:

- `test_log_tools.py`와 `test_unified_agents.py`가 `unified_controller_router`를 import하고
  있어, 컨트롤러만 지우면 테스트 두 개가 깨지는 상태였다
- `src/tools/source.py::add_source_and_embed`, `src/embedding_optimizer.py`,
  `router.py::/source_manager`, `user_page.html`의 미사용 JS 5개가 함께 죽어 있었다
- 그 정리 과정에서 `/call_agent`의 필드명 불일치를 발견해 기록만 남겼다 — 이것이 v0.4에서
  "422가 성공으로 보인" 문제로 다시 나온다

**남은 결과**: 도구 9개를 문 단일 Agent 하나. 도구를 늘려도 라우팅 표를 고칠 필요가 없다.

### 그 밖에

- **Gemini → Anthropic Claude** (`ab18a51`) — 쿼터 한도 회피가 직접적인 이유였다
- **임베딩은 Google → 로컬 HuggingFace** (`95cf81a`) — `jhgan/ko-sroberta-multitask`.
  한국어 특화, 로컬 실행이라 무료·무제한
- **대화 기록**: `InMemorySaver` + `thread_id = user_id`. 재시작하면 사라지는 것을 알고
  택한 것이고, 지금도 그대로다(「알려진 한계」 참조)

---

## v0.1 — 구조 설계 (2026-07-09 ~ 07-24)

코드보다 설계 문서가 먼저 쌓인 구간이다. `2026-07-24-refactoring-design.md` 2,236줄,
`2026-07-24-phase1-infrastructure.md` 1,733줄.

**최초 구조**

```
main.py → src/router.py → src/controller.py
                              ↓
                    src/llm_router.py (모델 스위칭)
                       - embedding: gemini
                       - llm: codex / gemini / claude
```

이 구간에 남은 작업 메모(기존 README의 「개발 일지」)에서 지금 봐도 의미 있는 것들:

- **2026-07-13 — codex OAuth는 messages가 streaming으로 온다** → 스트리밍 처리 시 `tool_calls`
  정보가 손실돼 `invoke`로 되돌렸다
- **2026-07-13 — 벡터DB 저장 시 TIL 파일명의 날짜를 추출해 내용에 추가** (유사도 검색 오류
  수정)
- **2026-07-13 — 날짜 벡터 유사도만으로는 검색이 어려워 메타데이터 필터 추가**
  ← **이 줄이 v0.4의 씨앗이다.** 필터는 이날 실제로 추가됐다. 그런데 그 필터가 조회하는
  `date` 키를 **쓰는 쪽 코드가 없었고**, 실패하면 조용히 유사도 검색으로 폴백하도록 되어 있어
  **약 3주 동안 한 번도 걸리지 않은 채로 정상처럼 보였다.**
- **2026-07-13 — ChromaDB 서버 모드** (다중 사용자 동시 접근): `HttpClient` 자동 감지 + 로컬
  모드 폴백, `docker-compose.yml`, 마이그레이션 스크립트
- **2026-07-14 — 멘토링**: 네이밍(사관원), 평가·검증 체계를 갖출 것

</details>

---

## 알려진 한계

2026-08-05에 코드를 직접 확인한 상태다. 상세는
[`docs/2026-08-03-04-작업회고.md`](docs/2026-08-03-04-작업회고.md#아직-해결되지-않은-것).

### `SHRINK_STEPS` 축약 — 고치지 않기로 한 것 (수용한 제약)

`src/tools/log.py:43`의 `SHRINK_STEPS = (3, 0)`은 v0.6 이후 의도대로 동작하지 않는다. 문제는
**커밋 날짜 묶기가 문서 구조를 바꿨다는 것**이다. 문서 본문은 이제 이렇다:

```
[2026-08-04] yuta_logagent — 커밋 4건

한 일:
- fix: keep git_log sources up to date     ← 이제 이게 커밋 메시지다
- feat: collect agent chat logs
```

`_shrink_commit`은 `- `로 시작하는 줄을 지운다. **예전엔 그게 파일 경로였는데, 지금은 커밋
메시지 본문이다.**

| 단계 | 결과 |
|---|---|
| `keep=3` | 커밋 메시지가 3개로 잘림 |
| `keep=0` | `if keep_files:`가 False라 "외 N개"도 안 붙고 **내용이 통째로 사라짐** |

부수 문제 둘:

- 사용자에게 나가는 안내문이 `"커밋의 파일 변경 목록을 생략했습니다"`인데, 실제로 지운 것은
  내용 전부다 — **사실과 다르다**
- `agent_chatlog`도 같은 `- 발화` 구조인데 축약 대상이 아니다(`source_type == "git_log"`
  조건). 축약되지 않고 **문서째로 버려진다**

**기록된 실측 피해**: 기간 조회(7/09~8/03)에서 59개 문서 중 35개만 표시, **7/28 이후 최신
29개 문서(47,930자)가 통째로 누락.** 날짜 정렬 때문에 최신부터 버려진다.

**테스트가 못 잡은 이유**: 30일치로는 `keep=3`(14,571자)에서 이미 예산 안에 들어와 `keep=0`
까지 가지 않는다. 90일치를 써야 재현된다.

#### 그런데 고치지 않기로 했다

`generate_daily_journals`(`b2b7b2c`)가 기간을 날짜별로 쪼개 처리하므로 **일지 생성이라는 주
경로는 이 로직을 타지 않는다.** 하루치가 예산을 넘길 일이 없기 때문이다. 남는 노출은 Agent가
`retriever_vectordb`를 기간으로 직접 호출하는 경우 하나뿐이고, 그 경로는 일지 생성의 주
흐름이 아니다.

판단: 실사용 경로가 우회하는 문제에 축약 로직 재설계(개명 포함 Task 6개)를 넣는 것보다, 같은
시간을 아직 손대지 않은 항목에 쓰는 편이 낫다.

**다만 남는 것을 분명히 해둔다.** 문제는 유실 자체보다 **유실을 알리지 않는다는 점**이다.
직접 호출 경로에서 내용이 날아가면 안내문은 여전히 `"커밋의 파일 변경 목록을 생략했습니다"`
라고 나가는데, 실제로 지운 것은 내용 전부다. 이 프로젝트가 v0.6 내내 고쳐온 것이 정확히
"조용히 실패하는 코드"였으므로, 이 항목은 **없앤 문제가 아니라 미룬 문제**로 남는다.

**착수하게 되면**: [`docs/gitlog-수집방식-변경계획.md`](docs/gitlog-수집방식-변경계획.md)에
Task 6개(TDD 구성)가 준비돼 있다. `SHRINK_STEPS`를 `(20, 5)`로 바꾸고 `keep=0` 제거,
`keep_files` → `keep_items` / `_shrink_commit` → `_shrink_daily` 개명, `agent_chatlog`도
축약 대상에 포함.

### `MAX_RESULT_CHARS`도 근거 없는 값이다

40,000자는 "하루치는 대개 수천 자"라는 추측이었다고 기록에 인정돼 있다. 실측으로는 92일 조회에
183,519자(4.6배)가 필요하다.

### 그 외

| 항목 | 상태 | 확인 |
|---|---|---|
| **C3** 삭제 파일 청크 잔류 | 미해결 | `_delete_stale_chunks`는 *다시 넣는* 문서만 처리한다. 삭제된 파일은 애초에 목록에 없어 영원히 남는다 |
| **memsearch 수집기** | 미구현 | `_collect_memsearch`가 `[], {}` 반환 |
| **작성자 필터** | 연기 | 팀 저장소에서 남의 커밋이 섞일 수 있다. 현재 등록 저장소는 본인 1명이라 미룸 |
| **`InMemorySaver`** | 설계상 한계 | 재시작하면 모든 대화가 사라진다. 워커를 여러 개 띄우면 워커별로 기억이 갈린다 |
| **모델명 하드코딩** | 미수정 | `router.py`의 `debugger_snapshot`이 `"Claude Sonnet 4.5"`를 문자열로 갖고 있다. 실제는 `claude-sonnet-5` |
| **`check_embeddings.py`** | 깨짐 | 메타데이터를 `source`/`content_hash`로 읽는데 수집기는 `file_path`/`file_hash`로 쓴다 |
| **CI에 테스트 게이트 없음** | 미착수 | 워크플로우가 빌드→배포만 하고 `pytest`를 돌리지 않는다 |
| **`docs/ARCHITECTURE.md`의 「알려진 이슈」** | 낡음 | 거기 적힌 `/sync_git_repo`·`/call_agent`·`log_maker.html`은 v0.4에서 이미 제거됐다. 현재 라우터에 남은 엔드포인트는 10개다 |

---

## 반복된 패턴

### 패턴 1 — "다 보지 않고 판단했다" (v0.5)

함정 9개 중 셋은 원인이 같다.

| 무엇을 다 보지 않았나 |
|---|
| 지표가 무엇을 세는지 — 분모에 문체가 섞인 것 |
| 원자료 — 4,000자 중 600자만 읽음 |
| 분모 분포 — 43일이 0이라는 것 |

**셋 다 실행하기 쉬운 결론으로 이어졌다는 점에서 위험했다** — "환각이다", "모델 차이가 없다".
편한 결론일수록 근거를 더 봐야 한다.

> 측정 도구를 만들 때 가장 먼저 확인할 것은 **분모가 무엇이고 그것이 타당한가**다.

### 패턴 2 — "조용히 실패했다" (v0.4, v0.6)

에러를 삼키고 폴백하거나, 성공 응답 안에서 조용히 자르거나, 유실을 세지 않는 코드가 반복해서
나왔다. 공통점은 **실패한 쪽이 아무 말도 하지 않는다**는 것이다.

그래서 수정의 방향도 하나로 모였다 — **실패를 소리 나게 만들기.**
